apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "storedog-service.fullname" . }}-test-connection"
  labels:
    {{- include "storedog-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "storedog-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

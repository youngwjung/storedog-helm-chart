apiVersion: v1
kind: Service
metadata:
  name: {{ include "storedog-service.fullname" . }}
  labels:
    {{- include "storedog-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "storedog-service.selectorLabels" . | nindent 4 }}
